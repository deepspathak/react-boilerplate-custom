/**
 *
 * {{ properCase name }} Component
 *
 */

import styled from 'styled-components';
import { mobileOnly } from '../../utils/styleUtils';

const color = 'red';

const {{ properCase name }} = styled.div`
  margin: 2em auto;
  ${mobileOnly`
    p {
      color: ${color};
    }
  `};
`;

export default {{ properCase name }};
